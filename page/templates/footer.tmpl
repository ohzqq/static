{{define "footer"}}
	{{range .ReadScripts}}
		<script>{{.}}</script>
	{{end}}
	{{if eq .Mime "image"}}
		{{template "swiperLightbox" "img"}}
	{{end}}
	{{if eq .Mime "video"}}
		{{template "swiperLightbox" "video"}}
	{{end}}
{{end}}
