{{define "bodyimage"}}

<h1>{{.Title}}</h1>

{{range Batch .Files}}
<div class="w3-row">
	{{range .}}
	<div class="thumbnail w3-quarter" lightbox-toggle>
		<img src="{{.}}">
	</div>
	{{end}}
</div>
{{end}}

{{end}}
